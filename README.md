# fabric-adf-databricks-retail-pipeline
Metadata‑driven incremental ingestion from on‑prem SQL Server to Azure Data Lake Gen2. ADF pipelines orchestrate Lookup, ForEach, Copy, Notebook, and Stored Procedure activities. Databricks PySpark applies medallion architecture (raw → bronze → silver → gold) with SCD logic.
